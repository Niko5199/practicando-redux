export const searchResult = (state) => state.results.data;
export const isLoadingResults = (state) => state.results.isLoading;
export const errorResults = (state) => state.results.error;
export const mealDetail = (state) => state.detail.data;
export const isLoadingDetail = (state) => state.detail.isLoading;
export const errorDetail = (state) => state.detail.error;
